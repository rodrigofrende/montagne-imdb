# OMDb API Configuration
# Get your free API key at: https://www.omdbapi.com/apikey.aspx
VITE_OMDB_API_KEY=your_api_key_here

# Note: This is a client-side app, so the API key will be exposed in the bundle.
# For production, consider using a serverless proxy to hide the key.
# See: /api/proxy.js for Vercel serverless function implementation.
